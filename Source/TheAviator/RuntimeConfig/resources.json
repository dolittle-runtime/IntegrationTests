{
    {{#each tenants}}
    "{{tenantId}}": {
        "eventStore": {
            "servers": [
                "{{server}}"
            ],
            "database": "event_store"
        }
    }
    {{/each}}
}